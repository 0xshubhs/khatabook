// Shared ESLint flat config. Kept intentionally empty in Phase 0 — the real
// rules (TypeScript, React, import ordering) are wired up alongside the first
// real code in later phases. Apps reference this via `@khatabook/eslint-config`.
/** @type {import('eslint').Linter.Config[]} */
module.exports = [];
